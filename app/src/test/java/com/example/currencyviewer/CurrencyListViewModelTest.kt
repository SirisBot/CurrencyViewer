package com.example.currencyviewer

import org.junit.Test

import org.junit.Assert.*

/**
 * Example local unit test, which will execute on the development machine (host).
 *
 * See [testing documentation](http://d.android.com/tools/testing).
 */
class CurrencyListViewModelTest {
    @Test
    fun `rate service should return list of rates when api response is successful`() {
        //TODO make this pass when implementation is done
        fail()
    }

    @Test
    fun `rate service should return an empty list when api response is unsuccessful`() {
        //TODO make this pass when implementation is done
        fail()
    }
}